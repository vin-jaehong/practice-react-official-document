export const SignUp = () => {
  return (
    <form
      onSubmit={() => {
        // e.preventDefault();
        alert('Submitting!');
      }}
    >
      <input />
      <button>Send</button>
    </form>
  );
};
